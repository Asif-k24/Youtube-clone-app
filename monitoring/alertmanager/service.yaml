apiVersion: v1
kind: Service
metadata:
  name: alertmanager
  namespace: monitoring
spec:
  type: NodePort
  selector:
    app: alertmanager
  ports:
    - name: web
      port: # 9093 change if different
      targetPort: # 9093 change if different
      nodePort: # 30903 change if different
