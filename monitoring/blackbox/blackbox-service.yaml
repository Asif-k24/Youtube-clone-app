apiVersion: v1
kind: Service
metadata:
  name: blackbox-exporter
  namespace: monitoring
spec:
  type: NodePort
  selector:
    app: blackbox-exporter
  ports:
    - name: http
      port: # 9115 change if different
      targetPort: # 9115 change if different
      nodePort: # 31115 change if different
